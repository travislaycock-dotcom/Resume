# Mechanical Engineering Portfolio (GitHub Pages)

This folder is ready to publish via **Deploy from a branch**.

**Upload these to your repo root:** `index.html`, `Aycock_Resume_v4.pdf`, `assets/`, `.nojekyll`.

Then set **Settings → Pages → Source = Deploy from a branch**, **Branch = main**, **Folder = /(root)**.
